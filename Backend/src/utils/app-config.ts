// Development config

class Config {

    // Database: 
    public host = "localhost"; 
    public user = "root"; 
    public password = ""; 
    public database = "jamoveo"; 

    // Server port: 
    public port = 3001;

}


const appConfig = new Config();

export default appConfig;